import { Button } from "@/styled";
import styled from "styled-components";

export const ForgotContent = styled.section`
  width: 80%;
  margin: 0 auto;

  > * {
    margin: 10px auto;
  }
`

export const Title = styled.h3`
  width: fit-content;
  margin: 10px auto;
`

export const SubmitButton = styled(Button)`
  width: 100%;
`